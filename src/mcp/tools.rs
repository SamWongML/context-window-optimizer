// Tool definitions and input types are co-located with the server handler
// in `server.rs` to keep the rmcp macro invocations together.
//
// This module is kept for future expansion (e.g., standalone tool helpers
// used outside the MCP server path).
